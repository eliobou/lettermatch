# Copy to .env (same folder as docker-compose.yml). docker compose reads it automatically.

# Free TMDB v3 API key for proper movie posters — https://www.themoviedb.org/settings/api
# Leave blank to fall back to Letterboxd's social images.
TMDB_API_KEY=
